"use strict"; // Treat all JS code as newer version

// alert("Hello") // We r using nodeJS, not browser

// number => 2 to power 53
//bigInt
//String => ""
// boolean => T/F
// null => standalone value
// Undefined 
// syumbol => unique




// Object

let name = "Abhisek"
let age = 22
let city = "kolkata"
let isadult = true
let marritalState = null  //type => Object
let coder = undefined //
console.table([typeof(name), typeof(age), typeof(city), typeof(isadult), typeof(marritalState), typeof(coder)]);
